export const dynamic = 'force-dynamic';

/**
 * Root not-found.tsx
 * Handles 404 at the root level. The middleware.ts redirects to /[lang] routes.
 */
export default function NotFound() {
  return null;
}
